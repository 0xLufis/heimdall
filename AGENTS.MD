Until further notice don't care about kacwards compatibility.
When needing to refactor code future extensibility is essential.
Include linting and formatting in the CI/CD pipeline.
Always write tests when implementing new features.
Cross service type safety is preferred when viable.
Don't use magic numbers.
Don't write unsecure code.
Keep eye-candy to a minimum.
Don't use code / libraries not free for enterprise use.
Keep visual styles, UX design, UI design and user flows consistent.
Don't use AI cliche visuals instead take note of google's material design.
Keep the UI/UX enterpise firendly.
Make sure all code is extensible and safe.
Make sure that use in restricted network enviroments is possbile.
Make sure once packaged the software can be used offline in an airgapped OT network.
Don't leak secrets.
Prefer encryption on storage, don't keep sensitive data as clear text.
Form follows function.
Don't use gradients for every visual detail keep it limited.
Make sure color coding is consistent.
Always write docs.
Always create development documentation.
Always annotate with JS-DOC, C# XML docs and all features alike.
Use https://conventionalbranch.org/ branch naming.
Always use git features.
Have a three round validation for changes. Make sure atleast 2 agents check it.
Try to always cross-reference changes to not break existing features.
Use semantic versioning.
The project is in development phase like a pre-alpha until further notice.
Always componetize features that appear multiple times.
Make sure to follow interface - implementation model.
Make sure to allow for future APIs to interface with the code.
Don't rely on state when not neccesary.
Don't store sensitive data on client when not neccesary, and keep it encrypted when not neccesary.
Do preliminary client side validation like sanitizing data but always validate on the server-side.
Store malformed data seperate.
Keep logs.
Keep change logs.
Make sure secrets are managed via the enviroment.
Keep change logs.
Try to keep ai chit-chat to a minimum in commits and docs.
Keep emoji use to an absolute minimum.
Create graphs, charts and trees when useful.
Make sure to comply with TISAX, NIS2 and other security standards.
Don't always agree with me.
Validate tasks against common-sense always.
Second guess my requests.
Think like a senior developer.
Keep code simple but don't compromise on performance.
Use versioned APIs and versioning in data when possbile.
Keep audit logs and user change logs.
High customizability and configurability is necessary.
Keep sane defaults.
Don't try to expose ports for fun.
Make sure dev,debug and deployed contexts differ, make the enviroment compatible for all.
Make sure the project is easily packaged,
Make sure the agent can be automatically deployed by tools like InTune and other MDM tooling for OT networks via eg. Microsofts software center.
